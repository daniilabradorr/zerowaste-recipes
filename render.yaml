services:
  - type: web
    name: zerowaste-recipes
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    preDeployCommand: python manage.py migrate --noinput
    startCommand: gunicorn zerowaste.wsgi --log-file -
